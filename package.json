{
	"name": "@witchcraft/nuxt-logger",
	"version": "0.0.8",
	"description": "Nuxt module for logging.",
	"repository": "witchcraftjs/nuxt-logger",
	"license": "MIT",
	"type": "module",
	"sideEffects": false,
	"exports": {
		".": {
			"types": "./dist/types.d.mts",
			"import": "./dist/module.mjs"
		},
		"./*": {
			"types": "./dist/runtime/*.d.ts",
			"import": "./dist/runtime/*.js"
		},
		"./electron": {
			"types": "./dist/runtime/electron/index.d.ts",
			"import": "./dist/runtime/electron/index.js"
		}
	},
	"files": [
		"dist",
		"src"
	],
	"scripts": {
		"prepare": "husky && pnpm build",
		"build": "nuxt-module-build prepare && nuxt-module-build build && nuxi build playground",
		"build:only": "nuxt-module-build build",
		"dev": "nuxi dev playground",
		"dev:prepare": "nuxt-module-build build --stub && nuxt-module-build prepare && nuxi prepare playground",
		"lint": "eslint \"{src,test,playground/app}/**/*.{ts,vue}\" \"*.{js,cjs,mjs,ts}\"",
		"lint:types": "vue-tsc --noEmit && cd playground && vue-tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest watch"
	},
	"//peerDependencies": "Because we need the real paths to the workers, they must both be in the app's node_modules",
	"peerDependencies": {
		"pino": "^9.8.0",
		"pino-pretty": "^11.2.2"
	},
	"dependencies": {
		"@alanscodelog/tsconfigs": "catalog:",
		"@alanscodelog/utils": "catalog:",
		"@nuxt/kit": "catalog:",
		"defu": "latest",
		"pino": "catalog:",
		"pino-pretty": "catalog:"
	},
	"devDependencies": {
		"@alanscodelog/eslint-config": "catalog:",
		"@alanscodelog/semantic-release-config": "catalog:",
		"@nuxt/eslint-config": "catalog:",
		"@nuxt/module-builder": "catalog:",
		"@nuxt/schema": "catalog:",
		"@nuxt/test-utils": "catalog:",
		"@types/node": "latest",
		"changelogen": "catalog:",
		"electron": "catalog:",
		"eslint": "catalog:",
		"husky": "catalog:",
		"nuxt": "catalog:",
		"semantic-release": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:",
		"vue-tsc": "catalog:"
	},
	"release": {
		"extends": [
			"@alanscodelog/semantic-release-config"
		]
	},
	"commitlint": {
		"extends": [
			"@alanscodelog"
		]
	},
	"publishConfig": {
		"access": "public"
	}
}
